
public class Ranger extends Human {
	private int healthOfRanger = 250;

	public Ranger(int str, int end, int agil, int intel, int per, String name, String gender) {
		super(str, end, agil, intel, per, name, gender);
	}
}
